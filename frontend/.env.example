# Server-side only. Do NOT prefix these with VITE_.
TOY_MACHINE_KEY=replace-with-a-random-key-at-least-32-characters
PAYMENTS_API_TARGET=http://127.0.0.1:8080
KIOSK_PORT=8444

# Vercel server-side function (use the Railway URL for PAYMENTS_API_TARGET).
# PAYMENTS_API_TARGET=https://toyrush-production.up.railway.app
# Separate random activation key, not TOY_MACHINE_KEY. Minimum 32 characters.
TERMINAL_ACCESS_KEY=
TERMINAL_ALLOWED_ORIGINS=https://www.toyfactory.dev.br,https://toyfactory.dev.br
