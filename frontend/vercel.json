{
  "framework": "vite",
  "installCommand": "npm ci",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/gateway?route=/:path*" },
    { "source": "/((?!api(?:/|$)|.*\\.).*)", "destination": "/index.html" }
  ]
}
